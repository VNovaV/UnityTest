# 05 · 校招面试专项

## 怎么用这份文件

| 部分 | 对应哪条线 | 什么时候用 |
|---|---|---|
| A 计算机与 C# 基础 | 原理线 | W1 开始，每天 40 分钟 |
| B Unity 引擎原理 | 原理线 | W4 开始 |
| C 手撕代码与算法 | 算法线 | W1 开始，每天 1-2 题 |
| D 项目讲述脚本 | 冲刺线 | W13 开始准备，W21 定型 |
| E 简历 | 冲刺线 | W16 初稿，W24 定稿 |
| F 投递节奏 | 冲刺线 | W24 开始 |
| G 模拟面试 | 冲刺线 | W17 开始，每周一次 |
| H 反问 / I 面试后动作 | 冲刺线 | 面试期 |

---

## A. 计算机与 C# 基础

### A1 高频（必须答得上来）

| 知识点 | 你要能说出的关键内容 | 常见追问 |
|---|---|---|
| 值类型 vs 引用类型 | 存储位置、赋值语义（拷贝值 vs 拷贝引用）、默认值 | struct 里放引用类型会怎样？ |
| 栈与堆 | 栈由系统自动管理，堆由 GC 管理 | 为什么 Unity 要区分托管堆和原生堆？ |
| 装箱与拆箱 | 值类型转 object 会在堆上分配，产生 GC | Unity 里哪里最容易装箱？（foreach 遍历 Dictionary、字符串拼接数字、枚举做字典 Key）|
| GC | 标记清除、分代；Unity 用的是非分代 Boehm；GC 触发会暂停主线程造成卡帧 | 怎么减少？（对象池、避免每帧 new、StringBuilder、用 struct）|
| string vs StringBuilder | string 不可变，每次拼接都产生新对象 | 什么时候 StringBuilder 反而更慢？ |
| 委托与事件 | 事件是委托的封装，外部只能 += 和 -=，不能直接调用或赋值 | 为什么要这层封装？ |
| 闭包与变量捕获 | lambda 捕获外部变量会生成闭包类并在堆上分配；for 循环变量的经典坑 | 怎么避免闭包分配？ |
| 接口 vs 抽象类 | 多实现 vs 单继承；接口不能有字段 | 什么时候选抽象类？ |
| struct vs class | 值语义 vs 引用语义 | 什么时候该用 struct？ |
| 泛型与约束 | 避免装箱、类型安全；where T : class / struct / new() | 泛型为什么能避免装箱？ |
| 重载 vs 重写 vs 隐藏 | virtual / override / new 的区别 | 不写 override 会发生什么？ |
| ref / out / in | 引用传递；out 必须在方法内赋值 | 和返回多个值比哪个好？ |
| async/await vs 协程 | 协程在主线程、由 Unity 驱动、**不是线程**；async/await 基于 Task，可能跑在线程池 | 什么时候该用哪个？ |
| 面向对象 | 封装、继承、多态；里氏替换；组合优于继承 | 举一个你项目里用多态的例子 |
| 设计模式 | 单例、观察者、工厂、策略、状态、装饰器、责任链 —— **要能说适用场景，不是背定义** | 你在项目里用了哪些？为什么？ |

### A2 中频（有时间就补）

- 反射与特性（Attribute）、性能代价
- 序列化的原理与限制
- LINQ 的延迟执行（IEnumerable 是惰性的）与开销
- List 扩容机制（翻倍）、Dictionary 实现（哈希桶 + 拉链）
- 哈希冲突的解决方式
- 线程与锁的基础概念
- 值传递与引用传递的准确说法

### A3 计算机基础（校招一定会问一点）

- **网络**：TCP vs UDP、三次握手四次挥手、HTTP 与 HTTPS、Socket、长连接与心跳
- **操作系统**：进程 vs 线程、线程与协程的区别、内存管理、虚拟内存、死锁的四个条件
- **数据结构复杂度表**：常见结构在增删改查上的复杂度，能张口就来

---

## B. Unity 引擎原理

### B1 高频（几乎每场都问）

| 原理点 | 你要能讲清的关键句 | 常见追问 |
|---|---|---|
| 生命周期与执行顺序 | Awake → OnEnable → Start → FixedUpdate → Update → LateUpdate → OnDisable → OnDestroy | Awake 和 Start 的区别？OnEnable 会执行几次？ |
| 协程原理 | 协程不是线程，是主线程上由 Unity 驱动的状态机；编译器把 yield 编译成 IEnumerator；Unity 每帧调 MoveNext 检查 Current | yield return null 和 WaitForEndOfFrame 的区别？协程能跨场景吗？ |
| Update / FixedUpdate / LateUpdate | 帧率相关 vs 固定步长物理 vs 帧末（相机）| 为什么物理要放 FixedUpdate？ |
| 资源加载三兄弟 | Resources（打进包体、无法单独卸载、官方已不推荐）/ AssetBundle（灵活但依赖管理复杂）/ Addressables（AB 的上层封装，引用计数 + 异步）| Addressables 的句柄什么时候释放？ |
| 内存与 GC | Unity 有托管堆和原生堆；GC 是非分代的；触发时卡帧 | 怎么定位是谁在分配？（Profiler 的 GC Alloc 列）|
| DrawCall 与合批 | 一次 DrawCall 是一批同材质同 Shader 的网格提交；静态合批（不动物体，烘焙成大网格，牺牲内存）/ 动态合批（顶点数限制，已不推荐）/ SRP Batcher（URP 下同 Shader 变体合批，不合并网格）/ GPU Instancing（同网格同材质多实例）| 为什么你的场景合批没生效？ |
| UGUI 原理 | Canvas 下元素变化触发 Rebuild（Layout + Graphic）；同一 Canvas 下任一元素变化会导致整块重建，所以**动静分离**；合批基于材质与层级 | 为什么频繁改 Text 会卡？ |
| 物理系统 | FixedUpdate 固定步长驱动；碰撞检测流程；Layer Collision Matrix 减少无效检测；射线用 LayerMask 过滤 | 固定步长改了会有什么影响？ |
| 对象池 | 为什么能省 GC；复用时必须重置哪三类状态 | 池子开多大？无限增长会怎样？ |
| 事件系统 | 解耦的代价是什么；订阅泄漏怎么产生和防止 | 事件和直接引用怎么选？ |
| 性能分析 | Profiler 的 CPU（主线程/渲染线程/GC）、GC Alloc、Rendering（SetPassCall / Batches）、Memory 各看什么 | 你的项目瓶颈在哪？怎么找到的？ |
| IL2CPP vs Mono | Mono 是 JIT；IL2CPP 是 IL 转 C++ 再编译成原生；AOT 意味着不能动态生成代码 | 为什么 iOS 不能像 Android 那样反射热更？ |
| 序列化 | 只能序列化 public 字段或带 SerializeField 的私有字段；不支持字典；多态需要 SerializeReference | SO 运行时修改会怎样？ |
| 场景管理 | LoadSceneAsync、Additive、DontDestroyOnLoad 的用途与坑 | 加载进度条为什么卡在 0.9？ |

### B2 中频（面试官看方向随机问）

Shader 基础、URP 与内置管线的差别、实时光照 vs 烘焙、图集与纹理压缩、音频压缩格式、
Time.timeScale 影响哪些东西、Editor 扩展、帧同步 vs 状态同步、热更新方案。

---

## C. 手撕代码与算法

### C1 Unity 手写题（按重要性排序）

| 题目 | 要点 | 频率 |
|---|---|---|
| 单例（普通 / 线程安全 / MonoBehaviour 版）| 双重检查锁；MonoBehaviour 单例的初始化时机与跨场景 | 高 |
| 对象池 | 泛型 + 创建委托 + 容量上限；Get 与 Release | 高 |
| 事件系统 / 消息中心 | 用事件类型做 Key；订阅与退订的生命周期 | 高 |
| 状态机 | 状态基类 + 转移；可扩展性 | 高 |
| LRU 缓存 | Dictionary + 双向链表 | 高 |
| 协程调度器 | 用 Update 驱动 IEnumerator，自己实现 MoveNext | 中（**区分度最高**）|
| 定时器 / 冷却管理器 | 帧驱动 + 剩余时间查询 | 中 |
| UI 面板栈 | 栈结构 + 生命周期 | 低 |
| 随机权重抽取 | 累积权重，进一步可用二分优化 | 低 |

### C2 算法手写题

- 快排与归并：**必须能手写**，并能分析复杂度与稳定性
- 二分查找及边界变体（找第一个大于等于 target 的位置）
- 链表：反转、环检测、合并两个有序链表
- 二叉树：前中后序遍历（递归 + 迭代）、层序遍历
- 字符串：最长无重复子串、有效括号、回文判断
- 双指针：三数之和、盛最多水的容器
- DP：爬楼梯、最长递增子序列、0-1 背包
- 堆：TopK 问题

### C3 LeetCode 路线

| 阶段 | 周次 | 内容 |
|---|---|---|
| 一 | W1-W3 | 数组 / 双指针 / 字符串 / 哈希 / 链表 |
| 二 | W4-W8 | 栈队列 / 递归分治 / 排序 / 二分 / 二叉树 |
| 三 | W9-W15 | 图 / 堆 / DP / 贪心 / 位运算 |
| 四 | W16-W26 | 高频题复习 + 手写题 + 白板训练 |

**目标线（中厂标准）**：
- 简单题 5 分钟内写出可编译的代码
- 中等题 15 分钟内
- 困难题不要求做出来，但要能看懂题解并讲清思路
- 累计 300 题左右是校招的舒服区间，不必刷到 500

---

## D. 项目讲述脚本

### D1 三个版本

| 版本 | 时长 | 用在哪 |
|---|---|---|
| 电梯版 | 1 分钟 | 自我介绍、HR 面 |
| 标准版 | 5 分钟 | 技术一面 |
| 深挖版 | 15 分钟 | 技术二面、交叉面 |

### D2 标准版（5 分钟）结构

1. **一句话定位（10s）**：「一个用 Unity 做的俯视角波次生存射击，核心是我自己从零实现的战斗框架」
2. **背景与目标（20s）**：为什么做、目标是什么
3. **我做了什么（60s）**：技术栈 + 我负责的模块（**不要念功能列表**）
4. **最大的技术难点（120s）**：现象 → 排查过程 → 方案 → 数据
5. **量化结果（30s）**：数字
6. **反思（30s）**：如果重做会改什么

### D3 必须准备的 6 个故事

1. 你解决过**最难的一个 bug**（要有排查过程，不是「我搜了一下就好了」）
2. 你做过的**最大一次性能优化**（要有前后数据）
3. 一次**架构决策**（为什么这么分层，代价是什么）
4. 一次**取舍**（为了什么砍掉了什么）
5. 一次**技术分歧**（怎么说服或者怎么被说服的）
6. **如果重做你会改什么**（展示反思能力）

### D4 深挖三层的准备方式（以对象池为例）

- **第一层**：你怎么实现的？
  → 泛型 Pool，用 Queue 存空闲对象，创建走工厂委托，超过容量上限就直接销毁。
- **第二层**：为什么用 Queue 不用 Stack？
  → 用 Stack 会反复复用同一个对象、其余长期闲置；Queue 轮转能让对象状态更均匀，也更容易暴露「没重置干净」的 bug。
- **第三层**：复用时最容易漏什么？
  → 三类：残留协程与计时器、残留事件订阅、残留物理速度与状态。我第一次就漏了刚体速度，导致子弹出池就乱飞。

**准备方式**：把你的每个技术点，自己连问三个「为什么」，问到答不出来为止。那个边界就是你必须补的洞。

### D5 自检方法

每周录一次 5 分钟讲述，自己回看。判断标准：
- 有没有出现「然后就做好了」这种跳过过程的话
- 有没有出现数字
- 有没有出现「我认为 / 我选择 / 我权衡 / 我砍掉了」这类体现主动性的词

---

## E. 简历

### E1 硬规则

- **一页，不超。**
- 不放照片，不写身高体重爱好
- 不写「熟练使用 Unity」这类形容词
- **项目经历放最前面**（校招也放最前，这是你唯一的差异化）
- 最上面放链接：GitHub / 演示视频 / 作品集

### E2 项目经历模板

项目名 ｜ 时间 ｜ 角色
- 一句话说明：做了什么、什么类型、什么规模
- 我负责：XX 模块（写清是「我做的」，不是「我们做的」）
- 技术亮点一：做了什么 + 用了什么方案 + **结果数字**
- 技术亮点二：同上
- 技术栈：Unity 2022 LTS / URP / Addressables / ...

### E3 量化写法对照

| 差 | 好 |
|---|---|
| 负责敌人 AI 的开发 | 用状态机重构敌人 AI，新增敌人类型的代码改动从 6 个文件降到 1 个配置文件 |
| 优化了游戏性能 | 通过对象池与图集合并，DrawCall 从 380 降到 96，帧率从 42 提到稳定 60 |
| 实现了存档系统 | 实现带版本号迁移的 JSON 存档，跨 2 个版本的旧存档可正常读取 |
| 使用 Addressables 加载资源 | 用 Addressables 替换 Resources，包体从 180MB 降到 96MB，加载耗时从 8s 降到 1.2s |

### E4 常见减分项

- 把教程项目当自己的项目写（面试官一问细节就露馅）
- 写了不懂的关键词（**写了就一定会被问**）
- 只有功能描述，没有技术含量
- 没有链接，或者代码仓库是空的、乱七八糟的
- 时间线有空白却不解释

---

## F. 投递节奏

### F1 校招时间线

| 批次 | 时间 | 特点 | 策略 |
|---|---|---|---|
| 提前批 | 6-7 月 | 岗位少但竞争小，挂了不影响正式批 | 大胆投，当练兵 |
| 正式批 | 8-10 月 | 主战场 | 分批投：前 5 家练手，中间是主力，最后留保底 |
| 补录 / 春招 | 次年 3-4 月 | 机会少，竞争也小 | 复盘秋招，针对性补漏 |

### F2 投递策略

- **不要一次全投完。** 先投 5 家不那么想去的，用真实面试找问题。
- 每投 10 家做一次复盘。
- 保持记录表，见 F3。

### F3 记录表字段

公司 ｜ 岗位 ｜ 投递日期 ｜ 渠道 ｜ 状态 ｜ 当前轮次 ｜ 结果 ｜ 挂在哪 ｜ 原因分析 ｜ 改进动作

### F4 每 10 家的复盘问题

- 笔试挂得多还是面试挂得多？
- 笔试挂在哪类题上？
- 面试挂在哪一环：项目讲不清 / 八股不会 / 手撕写不出 / 表达混乱 / 反问没准备？

---

## G. 模拟面试

- **频率**：W17 开始每周一次
- **形式**：找人陪你，或者对着摄像头自己讲，**必须录像**
- **内容**：自我介绍 → 项目讲述 5 分钟 → 三个八股问题 → 一道手撕题
- **复盘**：24 小时内写完，只写「下次要改的一件事」

### 高频挂点与对策

| 挂点 | 表现 | 对策 |
|---|---|---|
| 项目讲不清 | 讲成功能列表，没有难点和数字 | 用 D2 的六段式重写脚本 |
| 八股只背不理解 | 背得出结论，答不出「为什么」 | 每个结论都追问自己三层 |
| 手撕写不出 | 平时只看不写 | 高频题手写至少三遍 |
| 表达混乱 | 想到哪说到哪 | 先说结论再展开（总-分-总）|
| 反问没准备 | 「没什么想问的」 | 准备 3 个问题，见 H |

---

## H. 反问面试官

按面试轮次挑 2-3 个：

- 团队现在做的是什么类型的项目？处在什么阶段？
- 新人进来一般会先负责哪部分？
- 团队目前的工程流程是怎样的（版本管理、打包、测试）？
- 您觉得这个岗位最关键的能力是什么？
- 团队最近在技术上遇到的挑战是什么？

**不要问**：加班多吗、能不能远程、涨薪幅度。这些留给 HR 面，而且要换委婉说法。

---

## I. 每场面试后 24 小时内要做的事

- [ ] 记录：公司、岗位、轮次、面试官问了哪些问题
- [ ] 把答不上来的问题**原样**写下来，立刻补
- [ ] 更新八股清单和项目讲述脚本
- [ ] 更新投递记录表
- [ ] 只挑一处改进（一次改太多等于没改）

---

*这份文件是求职版的核心增量。作品线决定你能不能进面，这份文件决定你能不能过面。*
